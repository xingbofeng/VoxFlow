# File transcription visual baselines

`file-transcription-signatures.json` stores normalized perceptual color-cell
signatures for the macOS-aligned Windows workbench states at 100%, 125%, 150%
and 200% DPI in both themes. The WPF regression test compares with a tolerance
so ClearType and Windows runner updates do not turn pixel noise into failures.
