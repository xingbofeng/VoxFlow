<UserControl x:Class="VoxFlow.Windows.App.Settings.SettingsShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:VoxFlow.Windows.App.Shell"
             xmlns:settings="clr-namespace:VoxFlow.Windows.App.Settings"
             Background="{DynamicResource PageBackgroundBrush}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type shell:GeneralSettingsPageViewModel}">
            <settings:GeneralSettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type shell:ModelsSettingsPageViewModel}">
            <settings:ModelsSettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type shell:VoiceSettingsPageViewModel}">
            <settings:VoiceSettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type shell:TranslationSettingsPageViewModel}">
            <settings:TranslationSettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type shell:ScreenshotSettingsPageViewModel}">
            <settings:ScreenshotSettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type shell:TextSettingsPageViewModel}">
            <settings:TextSettingsView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid x:Name="SettingsLayout">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource SidebarBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="18,24">
                <StackPanel DockPanel.Dock="Top" Margin="8,0,8,20">
                    <TextBlock Text="{Binding Heading}"
                               FontSize="{DynamicResource TitleFontSize}"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding Subtitle}"
                               Margin="0,6,0,0"
                               FontSize="{DynamicResource CaptionFontSize}"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               TextWrapping="Wrap" />
                </StackPanel>
                <ListBox ItemsSource="{Binding NavigationItems}"
                         SelectedValue="{Binding CurrentRoute, Mode=TwoWay}"
                         SelectedValuePath="Route"
                         Background="Transparent"
                         BorderThickness="0"
                         HorizontalContentAlignment="Stretch">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,0,0,6" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Selection"
                                                CornerRadius="{DynamicResource RowCornerRadius}"
                                                Padding="12,10"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="3,1,1,1">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Selection" Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
                                                <Setter TargetName="Selection" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type shell:SettingsNavigationItemViewModel}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Glyph}"
                                           Width="26"
                                           FontFamily="Segoe MDL2 Assets"
                                           VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Label}"
                                           Margin="8,0,0,0"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border x:Name="SettingsContentHost" Grid.Column="1" Padding="36,32">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          HorizontalContentAlignment="Stretch"
                          CanContentScroll="False"
                          Focusable="False">
                <ContentControl Content="{Binding CurrentPage}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Top" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
