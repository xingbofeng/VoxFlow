<ResourceDictionary x:Class="VoxFlow.Windows.App.Theming.Resources.SharedThemeResources"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">
    <sys:Double x:Key="PageSpacing">28</sys:Double>
    <sys:Double x:Key="SectionSpacing">18</sys:Double>
    <sys:Double x:Key="GridSpacing">12</sys:Double>
    <sys:Double x:Key="CardSpacing">16</sys:Double>

    <sys:Double x:Key="CardRadius">12</sys:Double>
    <sys:Double x:Key="ControlRadius">8</sys:Double>
    <sys:Double x:Key="RowRadius">10</sys:Double>
    <sys:Double x:Key="PanelBorderWidth">1</sys:Double>

    <sys:Double x:Key="CardShadowBlurRadius">8</sys:Double>
    <sys:Double x:Key="CardShadowOffsetY">3</sys:Double>
    <sys:Double x:Key="CardShadowOpacity">0.045</sys:Double>

    <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>
    <sys:Double x:Key="HeadingFontSize">24</sys:Double>
    <sys:Double x:Key="TitleFontSize">18</sys:Double>
    <sys:Double x:Key="BodyFontSize">14</sys:Double>
    <sys:Double x:Key="CaptionFontSize">12</sys:Double>

    <CornerRadius x:Key="CardCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="ControlCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="RowCornerRadius">10</CornerRadius>
    <Thickness x:Key="PanelBorderThickness">1</Thickness>
    <Thickness x:Key="CardPadding">16</Thickness>

    <Style x:Key="VoxFlowButtonStyle" TargetType="Button">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,9" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource ControlCornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VoxFlowCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource PanelBorderThickness}" />
        <Setter Property="CornerRadius" Value="{DynamicResource CardCornerRadius}" />
        <Setter Property="Padding" Value="{DynamicResource CardPadding}" />
    </Style>

    <Style x:Key="VoxFlowToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource PanelBorderThickness}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>
</ResourceDictionary>
