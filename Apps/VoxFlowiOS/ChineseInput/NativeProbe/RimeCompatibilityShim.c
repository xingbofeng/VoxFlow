#include "../Upstream/RimeKit/Sources/C/rime_api.h"

Bool RimeReplaceInput(RimeSessionId session_id, size_t pos, size_t count, const char *replace_char) {
  (void)session_id;
  (void)pos;
  (void)count;
  (void)replace_char;
  return False;
}
