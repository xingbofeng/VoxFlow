# Implementation Tasks: 应用路由、可靠语音任务与全应用“帮我说”

**Change ID:** `add-context-aware-voice-workflows`

---

## Phase 0: 规格与基线

- [x] 0.1 更新 `CONTEXT.md`，补充 VoiceTask、语音转录、“帮我说”、上下文快照、应用推荐来源等领域术语。
- [x] 0.2 更新 `docs/PRD.md`，写入本 change 的产品边界和验收目标。
- [x] 0.3 记录现有快捷键、历史表迁移版本、风格规则和注入行为基线。
- [x] 0.4 为现有右 Command 转录主链补充回归测试，锁定兼容行为。

**Quality Gate:**

- [x] 文档术语与 delta spec 一致。
- [x] 现有测试通过。

---

## Phase 1: VoiceTask 数据与状态机

- [x] 1.1 Red：为 VoiceTask 模式、阶段、状态、输出结果和失败原因编写模型测试。
- [x] 1.2 Green：新增 `VoiceTask`、`VoiceTaskMode`、`VoiceTaskStage`、`VoiceTaskStatus` 和结构化 failure 类型。
- [x] 1.3 Red：编写 VoiceTask SQLite 创建、阶段更新、未完成查询和旧库迁移测试。
- [x] 1.4 Green：新增数据库迁移和 `VoiceTaskRepository`，支持阶段性 upsert。
- [x] 1.5 定义音频文件生命周期和 Application Support 路径。
- [x] 1.6 Red：编写成功音频删除、失败音频保留和 24 小时清理测试。
- [x] 1.7 Green：实现任务音频清理服务。
- [x] 1.8 Refactor：确认 VoiceTask 与现有 `DictationHistoryEntry` 的职责边界，避免双份业务逻辑。

**Quality Gate:**

- [x] SQLite 可从当前 schema 无损升级。
- [x] 每个合法和非法状态转换均有测试。
- [x] 音频清理使用可注入 clock，不依赖真实等待。

---

## Phase 2: 可靠任务编排与输出保护

- [x] 2.1 Red：为录音开始建任务、ASR 后存原文、LLM 后存结果、输出后完成任务编写编排测试。
- [x] 2.2 Green：新增 `VoiceTaskCoordinator` 或最小化扩展现有 `DictationOrchestrator`，在关键阶段写入任务仓库。
- [x] 2.3 扩展 `DictationTarget`，记录 PID、Bundle ID、应用名和可用窗口标识。
- [x] 2.4 Red：编写目标应用不变、应用切换、窗口变化和目标退出场景测试。
- [x] 2.5 Green：实现输出前目标窗口校验。
- [x] 2.6 将 `TextInjector.inject` 改为返回结构化结果，而不是无返回值。
- [x] 2.7 新增 `OutputService`，统一普通转录注入、窗口变化复制和失败复制。
- [x] 2.8 Red：编写 LLM 失败回退原始转写、注入失败保留剪贴板、复制失败可恢复测试。
- [x] 2.9 Green：实现降级链并记录结构化失败原因。
- [x] 2.10 实现启动时未完成任务检测，仅发出提示，不自动重试。

**Quality Gate:**

- [x] 普通转录主链行为保持兼容。
- [x] 不存在目标变化后仍发送 Command-V 的路径。
- [x] 每个输出失败场景都保留至少一份可恢复文本。

---

## Phase 3: 已安装应用扫描与内置注册表

- [x] 3.1 Red：为 `/Applications`、`~/Applications` 和系统应用扫描去重编写测试。
- [x] 3.2 Green：实现 `InstalledApplicationProvider`，输出名称、Bundle ID、图标引用、路径和系统分类。
- [x] 3.3 定义 `KnownApplicationRegistry` 数据格式和版本字段。
- [x] 3.4 添加第一批高频应用预设，覆盖聊天、邮件、编程、终端、写作、办公、浏览器和通用类别。
- [x] 3.5 Red：编写多 Bundle ID、大小写、重复安装和注册表未命中测试。
- [x] 3.6 Green：实现 Bundle ID 精确匹配和建议来源标记。
- [x] 3.7 明确 Terminal、iTerm、Ghostty 默认映射到 `builtin.coding`。

**Quality Gate:**

- [x] 扫描过程不读取用户文档和应用内容。
- [x] 注册表是数据驱动的，不在视图或 orchestrator 中散落应用判断。
- [x] 未知应用不会阻断列表展示。

---

## Phase 4: LLM 批量分类与确认流程

- [x] 4.1 Red：为批量分类 Prompt、严格候选 style ID 和 JSON 解码编写测试。
- [x] 4.2 将现有单应用 `ApplicationStyleClassifier` 扩展或替换为批量推荐接口。
- [x] 4.3 仅向 LLM 发送名称、Bundle ID 和系统分类。
- [x] 4.4 合并注册表结果、AI 推荐和默认结果，保留来源与置信度。
- [x] 4.5 Red：编写用户规则、已确认 AI、注册表和默认风格的优先级测试。
- [x] 4.6 Green：实现推荐预览模型；确认前不写入 `AppStyleRuleStore`。
- [x] 4.7 实现 LLM 配置并测试成功后的单次邀请状态。
- [x] 4.8 处理跳过、模型不可用、请求超时、部分结果和非法 style ID。

**Quality Gate:**

- [x] 分类失败不影响普通转录。
- [x] 推荐结果必须经用户确认才成为运行时规则。
- [x] LLM 不可返回候选风格以外的持久化值。

---

## Phase 5: 应用规则 UI

- [x] 5.1 Red：为智能配置邀请的出现、跳过和不重复出现编写 ViewModel 测试。
- [x] 5.2 实现 LLM 配置成功后的“智能配置应用”邀请。
- [x] 5.3 实现扫描和分类进度状态，以及取消和错误状态。
- [x] 5.4 实现按风格分组的推荐预览，区分“系统预设”和“AI 推荐”。
- [x] 5.5 支持预览中移动应用、移除建议和确认应用配置。
- [x] 5.6 在风格详情增加“适用应用”区域和应用图标列表。
- [x] 5.7 实现已安装应用选择器：搜索、添加、移除、浏览 `.app`、重置建议。
- [x] 5.8 Red：为一个应用只属于一个风格、移动后解除旧绑定编写测试。

**Quality Gate:**

- [x] 列表在大量已安装应用时保持可滚动和可搜索。
- [x] 所有操作都有成功、空状态、加载和错误反馈。
- [x] 用户现有规则不被重新扫描静默覆盖。

---

## Phase 6: 快捷键动作绑定

- [x] 6.1 Red：为转录和“帮我说”动作绑定、冲突检测和迁移现有快捷键编写测试。
- [x] 6.2 定义 `VoiceAction` 与触发方式模型，保留当前右 Command 配置。
- [x] 6.3 实现“长按槽位可用时推荐帮我说，否则设置独立快捷键”的策略。
- [x] 6.4 更新设置页，分别展示语音转录与“帮我说”的绑定状态。
- [x] 6.5 无“帮我说”快捷键时显示设置入口，不静默失败。
- [x] 6.6 确保两个动作不能绑定到无法区分的同一触发。

**Quality Gate:**

- [x] 旧设置升级后仍可按原快捷键转录。
- [x] 快捷键冲突在保存前可见并被阻止。

---

## Phase 7: 通用上下文采集

- [x] 7.1 定义 `ContextSnapshot`、来源、窗口元数据、裁剪结果和采集警告。
- [x] 7.2 Red：为超时、空结果、重复文本、最大长度和安全输入区域编写测试。
- [x] 7.3 实现当前应用、窗口标题和目标窗口标识采集。
- [x] 7.4 实现 Accessibility 可见文本、选中文本和当前输入区域采集。
- [x] 7.5 实现文本去重、噪声过滤、长度限制和来源标注。
- [x] 7.6 设计并实现当前窗口视觉上下文兜底，优先复用支持视觉输入的现有 LLM Provider。
- [x] 7.7 不支持视觉输入时跳过视觉兜底并记录警告。
- [x] 7.8 确保不自动滚动窗口、不读取安全输入框、不保存截图。
- [x] 7.9 使用有界超时，保证上下文采集不阻塞录音和 ASR。

**Quality Gate:**

- [x] 上下文采集在主线程外执行且不影响录音启动。
- [x] 安全输入区域测试证明不会生成可上传快照。
- [x] 截图生命周期只存在于单次任务内。

---

## Phase 8: “帮我说”生成链

- [x] 8.1 Red：为固定 Agent Prompt 的任务类型、忠实性、仅输出正文和上下文不足规则编写测试。
- [x] 8.2 实现 `AgentPromptBuilder`，输入应用元数据、风格指导、上下文和用户口述。
- [x] 8.3 Red：为录音与上下文并行、上下文失败降级、LLM 失败可重试编写测试。
- [x] 8.4 实现 `agentCompose` 任务编排。
- [x] 8.5 成功结果写入系统剪贴板并保持，不恢复旧剪贴板。
- [x] 8.6 明确禁止 `agentCompose` 自动注入、模拟 Enter 或发送消息。
- [x] 8.7 未配置 LLM 时返回可操作错误并引导配置，不影响普通转录。
- [x] 8.8 记录上下文快照、生成结果、trace、复制结果和失败阶段。

**Quality Gate:**

- [x] “帮我说”可从任意前台应用触发。
- [x] 上下文失败时仍可仅凭口述生成。
- [x] 成功路径没有键盘注入或自动发送事件。

---

## Phase 9: HUD、首页详情与恢复操作

- [x] 9.1 扩展 HUD 状态：读取窗口、已读上下文、转写、生成、已复制和降级。
- [x] 9.2 保持 HUD 紧凑，长错误文案换行且不遮挡当前应用主要区域。
- [x] 9.3 扩展首页历史项和详情模型，展示任务模式、状态、窗口、上下文和输出结果。
- [x] 9.4 在首页详情显示经过裁剪的上下文预览，不显示或保存截图。
- [x] 9.5 按可用数据提供复制、再次注入、重新生成、重新转写和删除。
- [x] 9.6 Red：为每种失败状态对应的可用操作编写 ViewModel 测试。
- [x] 9.7 实现未完成任务启动提示和筛选入口。

**Quality Gate:**

- [x] 现有历史详情和 LLM trace 仍可查看。
- [x] 没有可恢复数据的操作不会显示或启用。
- [x] 重试不会静默覆盖原始口述和旧结果。

---

## Phase 10: 集成、隐私与发布验证

- [x] 10.1 更新权限说明，区分 Accessibility 与屏幕录制用途。
- [x] 10.2 确认仅在首次需要视觉上下文时申请屏幕录制权限。
- [x] 10.3 增加日志脱敏，禁止记录上下文正文、截图和 API key。
- [x] 10.4 添加端到端集成测试：普通转录成功、窗口变化、Agent 成功、上下文失败、LLM 失败和重启恢复。
- [x] 10.5 使用真实 macOS App 手工验证至少聊天、浏览器、IDE、Terminal 和文本编辑器五类应用。
- [x] 10.6 使用 Browser 检查本地 Web 目标（如本 change 引入）；原生 macOS 界面使用可用的桌面验证路径。
- [x] 10.7 验证按下后 100ms 内录音、上下文 500ms 目标和输出反馈延迟。
- [x] 10.8 更新帮助文档、隐私说明和用户可见变更记录。
- [x] 10.9 按项目规范更新 `implementation-notes.md`；超过维护长度时先归档。

**Quality Gate:**

- [x] `swift test` 通过。
- [x] `swift build -c debug -Xswiftc -warnings-as-errors` 通过。
- [x] `make build` 通过并验证签名和资源。
- [x] 真实调用、mock、静态检查、手工检查和未验证项在最终报告中分别列明。

---

## Completion Checklist

- [x] 所有 phase 完成。
- [x] 所有 quality gate 通过。
- [x] PRD、CONTEXT、帮助和隐私文档同步。
- [x] 没有自动发送或“帮我说”自动注入行为。
- [x] 用户现有快捷键和应用规则完成兼容迁移。
- [x] 已执行提交前完整 review。
- [x] 准备进入 `/openspec-archive`。
